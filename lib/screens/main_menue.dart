import 'package:flutter/material.dart';

class MainMenueScreen extends StatelessWidget {
  const MainMenueScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column(crossAxisAlignment: ,));
  }
}
